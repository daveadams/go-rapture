package main

import (
	"github.com/daveadams/go-rapture/log"
)

// Cleans up old cached creds from unused sessions
func CommandClean(cmd string, args []string) int {
	log.Tracef("[main] CommandClean(cmd='%s', args=%s)", cmd, args)

	shgen.Echo("Rapture Clean COMING SOON")

	return 0
}
